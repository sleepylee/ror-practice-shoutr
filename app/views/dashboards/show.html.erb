<h1>Control Dashboard</h1>

<%= form_for @shout do |form| %>
  <%= form.text_field :body, placeholder: "Tweet it here", required: true %>
  <%= form.submit "Tweeted!" %>
<% end %>

<%= render @shouts %>